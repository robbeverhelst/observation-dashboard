services:
  app:
    image: ${IMAGE_NAME:-ghcr.io/robbeverhelst/observation-dashboard:latest}
    ports:
      - '${PORT:-3000}:3000'
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
